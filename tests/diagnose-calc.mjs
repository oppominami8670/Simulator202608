import { chromium } from 'playwright';
import fs from 'node:fs';
import path from 'node:path';
import { pathToFileURL } from 'node:url';
const file=path.join(process.cwd(),'phase9.html'),browser=await chromium.launch({headless:true}),page=await browser.newPage(),errors=[];page.on('pageerror',e=>errors.push({message:e.message,stack:e.stack}));await page.goto(pathToFileURL(file).href,{waitUntil:'domcontentloaded',timeout:30000});await page.waitForTimeout(1200);
const result=await page.evaluate(async()=>{const w=m=>new Promise(r=>setTimeout(r,m)),script=[...document.scripts].map(s=>s.textContent||'').find(s=>s.includes('function calc'))||'',marker=script.indexOf('function calc'),calcSource=typeof calc==='function'?calc.toString():'';const setFirst=id=>{const s=document.getElementById(id),o=[...(s?.options||[])].find(x=>!x.disabled&&x.value!=='');if(!s||!o)throw Error(`#${id} has no usable option`);s.value=o.value;s.dispatchEvent(new Event('change',{bubbles:true}))};let runtime;try{if(Object.keys(GLOBAL_DATA||{}).length!==6)throw Error('GLOBAL_DATA does not contain 6 carriers');setFirst('carrier');await w(50);setFirst('category');await w(50);setFirst('device');await w(50);setFirst('ins');await w(50);setFirst('con');await w(50);setFirst('plan');await w(50);setFirst('data');await w(50);const r=calc();if(!r||!Number.isFinite(r.first)||!Number.isFinite(r.later))throw Error('calc() did not return numeric first/later');runtime={status:'PASS',result:r}}catch(e){runtime={status:'FAIL',name:e.name,message:e.message,stack:e.stack||null}}return{carriers:Object.keys(GLOBAL_DATA||{}),scriptLength:script.length,calcMarker:marker,calcSourceLength:calcSource.length,runtime,calcHead:calcSource.slice(0,500),calcTail:calcSource.slice(-500)}});
console.log('[DIAG] page errors:',JSON.stringify(errors,null,2));console.log('[DIAG] runtime:',JSON.stringify(result.runtime,null,2));console.log('[DIAG] carriers:',result.carriers.length);console.log('[DIAG] calcMarker:',result.calcMarker);console.log('[DIAG] calcSourceLength:',result.calcSourceLength);fs.writeFileSync('calc-diagnostic.json',JSON.stringify({errors,...result},null,2));await browser.close();if(errors.length||result.runtime.status!=='PASS')process.exit(1);
